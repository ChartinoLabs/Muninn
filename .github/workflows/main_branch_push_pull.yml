---
name: Muninn main branch push or PR
on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check-changelog-fragment:
    if: >-
      github.event_name == 'pull_request' &&
      github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Check for changelog fragment
        env:
          BASE_REF: ${{ github.base_ref }}
          PR_NUMBER: ${{ github.event.number }}
        run: |
          FRAGMENT_TYPES="parser_added|parser_updated|parser_fixed|core_added|core_fixed|breaking|internal"
          FRAGMENTS=$(git diff --name-only --diff-filter=A \
            "origin/${BASE_REF}...HEAD" \
            -- 'changes/*' \
            | grep -E "\.(${FRAGMENT_TYPES})$" || true)
          if [ -z "$FRAGMENTS" ]; then
            echo "::error::No changelog fragment found. Add a fragment file in" \
              "changes/ (for example, changes/${PR_NUMBER}.core_fixed)." \
              "See changes/README.md for details."
            exit 1
          fi

          EMPTY_FRAGMENTS=""
          while IFS= read -r fragment; do
            [ -n "$fragment" ] || continue

            if ! grep -q '[^[:space:]]' "$fragment"; then
              EMPTY_FRAGMENTS="${EMPTY_FRAGMENTS}${fragment}\n"
            fi
          done <<EOF
          $FRAGMENTS
          EOF

          if [ -n "$EMPTY_FRAGMENTS" ]; then
            echo "::error::Empty changelog fragment(s) found. Add a user-facing" \
              "release note to each fragment file:"
            printf '%b' "$EMPTY_FRAGMENTS"
            exit 1
          fi

          echo "Found changelog fragment(s):"
          echo "$FRAGMENTS"
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6
      - uses: ./.github/actions/setup-python-uv
        with:
          python-version: "3.13"
      - name: Format with ruff
        run: uv run ruff format --check .
  lint-python:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6
      - uses: ./.github/actions/setup-python-uv
        with:
          python-version: "3.13"
      - name: Lint with ruff
        run: uv run ruff check .
  security-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6
      - uses: ./.github/actions/setup-python-uv
        with:
          python-version: "3.13"
      - name: Security scan with bandit
        run: uv run bandit -r src/
  check-complexity:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6
      - uses: ./.github/actions/setup-python-uv
        with:
          python-version: "3.13"
      - name: Check code complexity with xenon
        run: uv run xenon --max-absolute B --max-modules B --max-average A src/
  check-types:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - name: Check out code
        uses: actions/checkout@v6
      - uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ matrix.python-version }}
      - name: Check type hints with ty
        run: uv run ty check .
  unit-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - name: Check out code
        uses: actions/checkout@v6
      - uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run unit tests with pytest
        run: uv run pytest --cov=src --cov-report=term-missing
  docs-build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-python-uv
        with:
          python-version: "3.13"
      - name: Install docs dependencies
        run: uv sync --group docs
      - name: Generate parser library data
        run: uv run python scripts/generate_catalog.py
      - name: Build docs (strict)
        run: uv run mkdocs build --strict
