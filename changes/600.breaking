IOS and IOS-XE `show mac address-table` now return `mac_table` as `vlan_key -> mac_address -> row`, with each row carrying `kind` (`unicast` or `multicast`) and no top-level `entries` / `multicast_entries` lists.
