Registered `show bfd session` on Cisco IOS-XR against the existing `show bfd sessions` parser. `show interfaces` now tolerates indented interface header lines, and `show bfd sessions` now parses rows whose timer columns are `n/a` or a bare detect time and whose state spans multiple words.
