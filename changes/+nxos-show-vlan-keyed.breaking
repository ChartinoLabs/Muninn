NX-OS `show vlan` now returns each VLAN's `ports` as a mapping of canonical interface name to `true` instead of a list, and primary private VLAN `secondary_vlans` as a mapping of secondary VLAN id to `true` instead of a list.
