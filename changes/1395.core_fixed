`ParseError` raised when every parser candidate fails now lists each attempted candidate (source and parser class, in order) along with why it failed, instead of a generic "no parser candidates produced a valid result" message.
