Raised the minimum supported `netutils` version to 1.15.0 to match the interface-name canonicalization behavior (e.g. `Ap`, `Di`, and colon-delimited `Se.../...:...` interface names) that parser fixtures were generated against, fixing `unit-test-lowest-deps` CI failures with the lowest pinned dependency versions.
