IOS `show lldp neighbors detail` now returns `neighbors` as a mapping of interface or port-id key to a single neighbor entry (not a list per key). Duplicate port labels without `Local Intf` are disambiguated with `::<chassis_id>`.
