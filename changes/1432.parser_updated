Registered interface-scoped `show interfaces <interface>` and `show ip interface <interface>` on Cisco IOS and IOS-XE, and extended `show ip interface` to capture multi-line `Helper addresses are` lists and the `Output features` line.
