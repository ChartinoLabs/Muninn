Registered interface-scoped `show interfaces <interface>` and `show ip interface <interface>` on Cisco IOS-XE, and extended `show ip interface` to capture multi-line `Helper addresses are` lists, wrapped `Multicast reserved groups joined` lists, and the `Output features` line.
